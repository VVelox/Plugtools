#!/bin/sh
#
# PROVIDE: mojo_nisaba
# REQUIRE: LOGIN NETWORKING
# KEYWORD: shutdown
#
# App::Nisaba LDAP admin web interface, served by Hypnotoad (Mojolicious'
# production preforking server, with zero-downtime hot restarts).
#
# Add the following to /etc/rc.conf (or /etc/rc.conf.d/mojo_nisaba) to
# enable and configure this service:
#
#   mojo_nisaba_enable="YES"
#   #mojo_nisaba_user="www"                       # service user
#   #mojo_nisaba_listen="http://*:8080"           # listen URL (NISABA_LISTEN)
#   # ...or serve HTTPS directly (requires IO::Socket::SSL; cert+key readable
#   # by the service user). Quote the whole value because of the "&":
#   #mojo_nisaba_listen="https://*:8443?cert=/usr/local/etc/nisaba/tls/server.crt&key=/usr/local/etc/nisaba/tls/server.key"
#   #mojo_nisaba_config="/usr/local/etc/nisabarc" # NISABA_CONFIG
#   #mojo_nisaba_secret=""                         # NISABA_SECRET (or set websecret in nisabarc)
#   #mojo_nisaba_hypnotoad="/usr/local/bin/hypnotoad"
#
# Hypnotoad tuning (leave empty to use Hypnotoad's own defaults):
#   #mojo_nisaba_workers="4"             # worker processes
#   #mojo_nisaba_clients="1000"          # max concurrent connections per worker
#   #mojo_nisaba_accepts="0"             # connections a worker accepts before stopping (0 = unlimited)
#   #mojo_nisaba_spare="2"               # spare workers kept warm during a hot restart
#   #mojo_nisaba_graceful_timeout="120"  # seconds to let a worker finish before killing it
#   #mojo_nisaba_heartbeat_timeout="50"  # seconds before an unresponsive worker is restarted
#   #mojo_nisaba_inactivity_timeout="15" # keep-alive connection idle timeout, seconds
#   #mojo_nisaba_proxy="1"               # trust reverse-proxy X-Forwarded-* headers
#
# "service mojo_nisaba reload" performs a zero-downtime hot restart (e.g. after
# an upgrade), keeping the listening sockets open across the swap.

. /etc/rc.subr

name="mojo_nisaba"
desc="App::Nisaba LDAP admin web interface"
rcvar="${name}_enable"

load_rc_config $name

: ${mojo_nisaba_enable:="NO"}
: ${mojo_nisaba_user:="www"}
: ${mojo_nisaba_group:="${mojo_nisaba_user}"}
: ${mojo_nisaba_listen:="http://*:8080"}
: ${mojo_nisaba_config:="/usr/local/etc/nisabarc"}
: ${mojo_nisaba_secret:=""}
: ${mojo_nisaba_hypnotoad:="/usr/local/bin/hypnotoad"}
: ${mojo_nisaba_workers:=""}
: ${mojo_nisaba_clients:=""}
: ${mojo_nisaba_accepts:=""}
: ${mojo_nisaba_spare:=""}
: ${mojo_nisaba_graceful_timeout:=""}
: ${mojo_nisaba_heartbeat_timeout:=""}
: ${mojo_nisaba_inactivity_timeout:=""}
: ${mojo_nisaba_proxy:=""}

# Writable state dir for the brute-force rate-limiter SQLite database
# (rateLimitPath). Only needed while rate limiting is enabled (the default).
: ${mojo_nisaba_dbdir:="/var/db/nisaba"}
rundir="/var/run/${name}"

program="/usr/local/bin/${name}"
pidfile="${rundir}/${name}.pid"
# Hypnotoad's own pid file (used for hot-restart coordination); must be writable
# by the service user, so it lives under the run directory rather than next to
# the application script (Hypnotoad's default).
hypnotoad_pidfile="${rundir}/hypnotoad.pid"

# daemon(8) supervises and restarts Hypnotoad. Hypnotoad reads its listen URL
# and all tuning from the environment (exported in the precmd below), so no
# per-URL argument quoting is needed here; "-f" keeps the Hypnotoad manager in
# the foreground so daemon(8) can supervise it.
command="/usr/sbin/daemon"
procname="/usr/sbin/daemon"
command_args="-S -t ${name} -R 5 -P ${pidfile} ${mojo_nisaba_hypnotoad} -f ${program}"

start_precmd="${name}_precmd"
extra_commands="reload"
reload_cmd="${name}_reload"

mojo_nisaba_setenv()
{
	[ -n "${mojo_nisaba_config}" ] && export NISABA_CONFIG="${mojo_nisaba_config}"
	[ -n "${mojo_nisaba_secret}" ] && export NISABA_SECRET="${mojo_nisaba_secret}"
	export NISABA_LISTEN="${mojo_nisaba_listen}"
	export NISABA_HYPNOTOAD_PID_FILE="${hypnotoad_pidfile}"
	[ -n "${mojo_nisaba_workers}" ]            && export NISABA_HYPNOTOAD_WORKERS="${mojo_nisaba_workers}"
	[ -n "${mojo_nisaba_clients}" ]            && export NISABA_HYPNOTOAD_CLIENTS="${mojo_nisaba_clients}"
	[ -n "${mojo_nisaba_accepts}" ]            && export NISABA_HYPNOTOAD_ACCEPTS="${mojo_nisaba_accepts}"
	[ -n "${mojo_nisaba_spare}" ]              && export NISABA_HYPNOTOAD_SPARE="${mojo_nisaba_spare}"
	[ -n "${mojo_nisaba_graceful_timeout}" ]   && export NISABA_HYPNOTOAD_GRACEFUL_TIMEOUT="${mojo_nisaba_graceful_timeout}"
	[ -n "${mojo_nisaba_heartbeat_timeout}" ]  && export NISABA_HYPNOTOAD_HEARTBEAT_TIMEOUT="${mojo_nisaba_heartbeat_timeout}"
	[ -n "${mojo_nisaba_inactivity_timeout}" ] && export NISABA_HYPNOTOAD_INACTIVITY_TIMEOUT="${mojo_nisaba_inactivity_timeout}"
	[ -n "${mojo_nisaba_proxy}" ]              && export NISABA_HYPNOTOAD_PROXY="${mojo_nisaba_proxy}"
	return 0
}

mojo_nisaba_precmd()
{
	# Run directory for the pid files, writable by the service user.
	install -d -o "${mojo_nisaba_user}" -g "${mojo_nisaba_group}" -m 0755 \
		"${rundir}"

	# State dir for the rate-limiter database, writable by the service user.
	install -d -o "${mojo_nisaba_user}" -g "${mojo_nisaba_group}" -m 0700 \
		"${mojo_nisaba_dbdir}"

	# Pass configuration to the application via the environment (preserved
	# through rc.subr's "su -m").
	mojo_nisaba_setenv
	return 0
}

# Zero-downtime hot restart: re-running Hypnotoad against the same application
# upgrades the running manager in place (it locates it via the pid file). "su -m"
# preserves the environment exported above.
mojo_nisaba_reload()
{
	mojo_nisaba_setenv
	su -m "${mojo_nisaba_user}" -c "${mojo_nisaba_hypnotoad} ${program}"
}

run_rc_command "$1"
