% title 'Sign In';
%= include 'partials/login_card', card_class => 'mt-4', heading => 'Sign In', submit_label => 'Sign In', login_post_url => url_for('sso_login_post')->query(rid => stash('sso_rid') // ''), passkey_start_url => url_for('sso_passkey_login_start'), passkey_finish_url => url_for('sso_passkey_login_finish'), totp_redirect_url => url_for('sso_totp_challenge')->query(rid => stash('sso_rid') // ''), success_redirect_url => url_for('sso_consent')->query(rid => stash('sso_rid') // '')
